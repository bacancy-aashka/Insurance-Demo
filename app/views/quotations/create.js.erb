<% if @quotation.errors.any? %>
  $("#error_fields").html("<%= escape_javascript(render partial: 'error' ) %>");
  $(window).scrollTop(0);
<% else %>
  $("#transaction_form").html("<%= escape_javascript(render partial: 'transaction_form' ) %>");
<% end %>